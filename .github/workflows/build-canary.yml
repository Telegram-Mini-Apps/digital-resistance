name: Build Canary

on:
  workflow_dispatch:
    inputs:

concurrency:
  group: canary
  cancel-in-progress: true

jobs:
  build:
    uses: ./.github/workflows/build-common.yml
    secrets: inherit
    with:
      image_tag: canary
